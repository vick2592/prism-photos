<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Prism is local-only. We exclude all app data from cloud backup. -->
    <exclude domain="database" path="." />
    <exclude domain="sharedpref" path="." />
    <exclude domain="file" path="." />
</full-backup-content>
