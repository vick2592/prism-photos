package dev.prism.gallery.di

// RepositoryModule is a placeholder for Phase 2+.
// MediaStoreRepository and UserPreferences use @Inject constructor with @Singleton,
// so Hilt wires them automatically without explicit @Provides bindings.
//
// Add interface → implementation bindings here when needed, e.g.:
//   @Binds fun bindRepository(impl: MediaStoreRepositoryImpl): MediaRepository
